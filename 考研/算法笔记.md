## 算法的定义
### 渐进复杂性分析
1.O， $\Omega$ , $\Theta$ 符号定义
1.1定义
渐进复杂性分析用于描述算法在输入规模趋于无穷大时的运行时间或空间复杂度，提供了算法效率的数学描述。
	·大O符号：表示算法运行时间的上界(最坏情况)
		定义：对函数f(n),g(n),如果存在常数c>0,$n_0$>0.使得对所有的$n\geq n_0$，有f(n) $\leq$ cg(n) 成立，则f(n)$\in$ O(g(n)) .
		表示(f(n))的增长速率不超过(g(n))
	·大$\Omega$符号：表示算法运行时间的下界（最好情况）
		定义：对函数f(n),g(n),如果存在常数c>0,$n_0$>0.使得对所有的$n\geq n_0$，有f(n) $\geq$ cg(n) 成立，则f(n)$\in$ O(g(n)) 
		表示(f(n))的增长速率不小于(g(n))
	·$\Theta$符号：表示算法运行时间的紧确界。
		定义：如果f(n)$\in$o(g(n)),且f(n)$\in\Omega(g(n))$,存在常数$c_1, c_2 >0 和 n_0 > 0$ 使得对所有的$n\geq n_0, 有c_1*g(n)\leq f(n)\leq c_2*g(n)$
2.分析给定算法的渐进复杂性
2.1主要包括时间复杂度和空间复杂度的分析
·时间复杂度的分析
	常用的多项式求和：
	$$\sum^{i=1}_{n}i = \frac{n(n+1)}{2} = \Theta(n^2)$$ 
	$$\sum^{n}_{i=1} i^2 = \frac{n(n+1)(2n+1)}{6}=\Theta(n^3)$$
### 递归算法复杂性分析
1. 主定理求解递归方程
   主定理适用于形如$T(n) = aT(\frac{n}{b})+f(n)$的递归方程，通过比较f(n)和$n^{log_ba}$ 快速求解
   ·识别参数：确定子问题数量（a），规模缩减因子（n/b）和非递归工作量（f（n）），计算${log_ba},得到 n^{log_ba}$
   
   ·比较 (f(n)) 和$n^{log_ba}$
	 ![[Pasted image 20250802225509.png]]  

2. 用递归树求解递归方程
   ·理解递归结构：
	   将递归方程分解为递归调用和非递归部分。
	   1.确定子问题数量（a）
	   2.规模缩减因子（n/b）
	   3.非递归工作量（f（n））。
   ·绘制递归树：
	   1.根节点表示原始问题T(n)
	   2.每层表示一次递归，子问题数量为$a^i$,规模为$n/b^i$
	   3.每层非递归的工作量为$a^i\cdot f(n/b^i)$ 
   ·确定树的深度
	  通常为$log_bn$ 
   ·计算总工作量：每层的非递归工作量之和
   ·求和并化简：累加完之后提取最高阶，得到渐进复杂度
   
3. 用迭代法求
   迭代法通过将递归方程逐层展开为级数，求和得到闭合形式，适合线性递归或简单递归。
练习：
例题1：求解下列递归方程的时间复杂度(用递归和主定理)$$T(n) = 2T(n/2) +nlogn, T(1) = O(1).$$
	1. 递归树法
	![[Pasted image 20250803214537.png|450]]
	 2.主定理法
	 ![[Pasted image 20250803214933.png|400]]

### 总结

	这章介绍了算法的基本概念，主要掌握渐进复杂性的定义和证明，了解渐进上下界和确界的证明，如何分析渐进复杂性，包括时间复杂度和空间复杂度，给出一个算法或递推方程，分析时间复杂度。
## 分而治之
1. 定义 
   分治算法将一个复杂问题分解为若干个规模较小的子问题，递归解决子问题，然后合并子问题的解以得到原问题的解。
### 求解常见的排序问题
1. 归并排序
   主要分为三步
   ·分解：将数组从中间分成两个数组，直到每个数组只有一个元素
   ·合并：合并连个已排序的子数组，形成一个新的有序数组
   ·递归：对上面的每个子数组进行排序，直到整个数组排序完成
   时间复杂度分析：
   ·递归方程：$$T(n) = 2T(n/2) + O(n)$$
   ·O（nlogn）
   空间复杂度：O(n）
   merge部分
   ![[Pasted image 20250804214548.png|400]]
2. 快速排序
   ·选择基准：选择一个元素，可以是数组中的任意一个
   ·分区操作：遍历数组， 小于基准的元素放到左侧，大于基准的放到右侧
   ·递归操作：对基准左侧的和右侧的子数组执行快速排序。
   partition部分
   ![[Pasted image 20250804214503.png]]
   初始化：在分区过程中，设置i = low - 1表示没有小于基准的元素
   遍历：通过for循环遍历从low到high-1的元素
   比较：如果当前元素小于基准，表示找到一个元素，增加i的值，并交换arr[i]和arr[j]。
   时间复杂度
   ·平均时间复杂度：(nlogn)
   ·最坏复杂度：O($n^2$ )
   ·空间复杂度(logn)
   
   在数组划分时固定主元位置，可以针对性构造最差情况
   在数组划分时可以选取随机位置主元，从而无法构造最差情况的例子
```cpp
int partition(vector<int>& arr, int low, int high) {
    int randomIndex = low + rand() % (high - low); // 随机选择基准
    swap(arr[randomIndex], arr[high]); // 将基准放到最后
    int pivot = arr[high]; // 选择最后一个元素作为基准
    int i = low - 1; // 小于基准的元素的索引
```
   随机化后的时间复杂度最好能达到O(nlogn)(基准正好每次在数组的中间位置)
### 最大子数组I
问题描述：给定一个数组，找到一个具有最大和的连续非空子数组，返回最大值
求解步骤
·分：将数组分成两半，依次求解这两个数组的最大值
·治：计算跨越中间子数组的最大和
·合：返回这三个数组中的最大值
求解跨中心元素数组最大和
![[Pasted image 20250804231254.png]]
时间复杂度为O(nlogn)
### 统计逆序数对
问题：给定一个数组寻找有多少对逆序对（逆序对是两个元素比较下标大小和数值大小符号相反）
·直接利用暴力搜索，对每个元素都与原数组元素比较n次 时间复杂度为O($n^2$)
·分而治之+排序：
	1.直接排序
	将原数组分为两半， 利用二分查找逆序对，查找的时间复杂度为$2\cdot\frac{n}{2}log\frac{n}{2}$,排序的时间复杂度为 $2\cdot\frac{n}{2}log\frac{n}{2}$    ,求解跨中心数组的逆序对时间复杂度为O(nlogn),递推方程为$$T(n) = 2T(n/2) + nlogn$$
	时间复杂度为$O(nlog^2n)$ 
	2.归并排序
	在归并排序中直接查找逆序对，利用排序的特性可以将拆分原数组的时间复杂度降为O(n),在排序过程中根据左子数组和右子数组交换的次数确定。$$T(n) =2T(n/2) + n$$
	时间复杂度为O(nlogn）
	![[fd901a79bb743da68e9d3356f027646e 1.png]]
### 次序选择问题
问题：次序选择问题（Order Statistics Problem）是一个经典的算法问题，主要是寻找一个数组中第 k 小的元素。
可以通过常规的排序解决，但时间复杂度可能比较大。
我们可以借鉴快速排序的思想降低时间复杂度
	·如果选择的基准索引等于k - 1，直接找到了目标元素
	·如果选择的基准索引大于k - 1，说明目标元素在基准的左边
	·如果选择的基准索引小于k - 1，说明目标元素在基准的右边
	因此不需要像快速排序一样递归左右子数组，只需要遍历包含第k个元素的数组。
	![[Pasted image 20250806121025.png]]时间复杂度如上
	
## 动态规划
### 01背包
问题输入：
·n个商品组成的集合O，每个商品有两个属性$v_i, p_i$ 分别表示体积和价格
·背包容量为C

输出：
·求解一个商品子集$S\subseteq O$ ,令$$\begin{align*}&max \sum_{{i\in S}}p_i  \\& s.t\sum_{i\in S}v_i \leq C\end{align*}$$
1. 蛮力枚举
   枚举所有搭配的可能![[Pasted image 20250807210517.png]]
   
   存在重复子问题
   优化：带备忘递归，构造备忘录P[i, c]表示在前i个商品中选择，容量背包为c时的最优解![[Pasted image 20250807211253.png]]
   ·**自顶向下递归+自底向上计算**
   ·在向上计算时如果P[i,c]不为空，直接返回存储的结果
2. 递推法
   不递归，直接求解P[i,c]
   递推公式:$$P[i,c] = max(P[i-1,c -v_i] + pi, P[i -1,c])$$
   ·从左往右，从上到下计算![[Pasted image 20250807212256.png]]
动态规划一般步骤：
·给出问题表示：p[i,c]：前i个物品可选，背包容量为c的最大总价格
	·明确原始问题：p[n,C] ：前n个物品可选，背包容量为C的最大总价格
·分析最优子结构
	·构造递推公式
·确定计算顺序
·**记录决策过程**
### 最大子数组
在分而治之中，将数组分为两个子数组，分别求解两个数组的最大值及跨越中间节点的子数组，并返回这三个之中的最大值，由此递归下去。

我们可以设计一个时间复杂度为O(n)的算法 
$D_i$: 表示x[i]开头的最大子数组
![[Pasted image 20250807222917.png]]
·证明情况1
![[Pasted image 20250814212444.png]]
·证明情况2

最后根据关系确定最优子结构，并得到递推公式
![[Pasted image 20250807223135.png]]
具体伪代码
![[Pasted image 20250807224023.png]]
```
输入: 数组 X[1..n]
输出: 最大子数组和

D[1] ← X[1]
max_sum ← D[1]

for i ← 2 to n do
    D[i] ← max(D[i-1] + X[i], X[i]) // 状态转移
    max_sum ← max(max_sum, D[i]) // 更新最大和

return max_sum
```

### 最长公共子序列
问题：给定两个子串X[1,...,n], Y[1, ...,m],求他们的最长公共子序列C[n,m]。
子序列不要求连续，但必须保持原序列的顺序。

·问题结构分析：
	C[i,j] ：表示X[1,...,n], Y[1, ...,m]的最长公共子序列的长度。
·递推关系建立
	考虑末尾字符串：
		1.若xi !=yj  去掉该字符串不会影响公共子序列的长度![[Pasted image 20250811225550.png]]
		2.若xi = yj,说明x和y的最后一个字符串相等,长度加一![[Pasted image 20250811225723.png]]
·自底向上计算
	初始化
	递推公式
·最优方案追踪
![[Pasted image 20250811230037.png]]
·代码
![[e3a6381a23f991c6347835e18400da80.png]]
时间复杂度为O(n$\cdot$m)
### 最长公共子串
子串的定义：给定序列中零个或多个连续的元素组成的子序列。
问题：
·输入：序列X[1,...,n],Y[1,...,m]
·输出：一个公共子串Z[1,...,L]

·问题结构分析
	C[i,j]表示在X[1,...,n],Y[1,...,m]中，以xi和yj结尾的最长公共子串Z[1,...L]的长度
·递推关系建立
	分析最优子结构
	情况1：xi != yj       x和y的末尾元素不相同，不存在公共子串c[i,j] =0,无子问题
	情况2：xi = yj         x和y的末尾元素相同，存在公共子串c[i,j] = c[i-1,j-1] + 1
	 综上构造递推公式：![[Pasted image 20250814214146.png]]
 ·自底向上计算
	 c[i,0] = c[0,j] = 0
 ·最优方案追踪
	 最长公共子串末尾位置和长度分别为为$p_{max}，l_{max}$
	  ![[Pasted image 20250814214553.png]]
时间复杂度O(n$\cdot$ m)
### 编辑距离问题
问题：通过有限次的编辑操作数，将A字符串变为B字符串
输入：长度为n的字符串s，长度为m的字符串t
输出：
求出一组编辑操作O=<e1,e2,...,ed>使字符串s经过O操作后变为t
- 问题结构分析
  D[𝒏, 𝒎]：字符串𝒔[𝟏. . 𝒏]变为𝒕[𝟏. . 𝒎]的最小编辑距离
- 递推关系建立
  - 对末尾元素进行删除
    D[i,j] = D[i, j - 1] + 1
  - 对末尾元素进行插入
    ![[Pasted image 20250815224107.png]]
	综合得出递推公式![[Pasted image 20250815224130.png]]
- 自底向下计算
- 最优方案追踪
  ![[Pasted image 20250815225810.png]]
### 钢条切割问题
输入：钢条长度n，价格表pl
输出：求解一组切割方案T = <c1,c2,...,cm>,是的切割后的钢条总价值最大

- 问题结构分析
  C[j] :表示切割长度为j的钢条可得最大总收益
  原始问题：c[n]：切割长度为n的钢条获得的最大收益
  可以在任意地方进行切割
- 递推关系建立
  可以选择切或者不切
  c[10] = max(c[10 - i] + p[i], p[10])   1 <= i <= 9
  进而得到
  c[j] = max(c[j - i] + p[i], p[j])   1 <= i <= 9
  ![[Pasted image 20250825113631.png]]

- 自底向上计算
  c[0] = 0   初始化
- 最优方案追踪
  rec[1,...,n]追踪数组
  - 不切 rec[j] = j    切割rec[j] = ki![[Pasted image 20250825120318.png]]
时间复杂度：O(n^2)  遍历钢条的长度，对切割后剩下的钢条长度进行遍历
### 矩阵链乘法问题
输入：
- n个矩阵组成的矩阵链$U_{1,...,n}= <U_1,U_2,...,U_n>$ 
- 矩阵链对应的维数p0，p1,...,pn，
输出：找到一种加括号的方式，确定矩阵乘法的计算顺序,使得相乘次数最小
![[Pasted image 20250829191945.png]]

- 最优子问题
  D[i,j]：计算矩阵Ui...j所许乘法的最小次数
  D[1,...,n]从1，...，n所需标量乘法的最小次数
  最优子结构：
  对Ui...j，若在k(i <= k < j)处切割，k可以取i- j个，问题拆分为![[Pasted image 20250829192427.png]]
- 递推关系建立
  ![[Pasted image 20250829192809.png]]当i = j时，只有一个矩阵Ui-1,i,乘法次数为0,且i < j只需考虑上三角
  ![[Pasted image 20250829193034.png]]
## 贪心
### 部分背包
问题描述：现有一容量为800ml的杯子，还有饮料对应的价格表，通过调制饮品使得饮品价格最高
![[Pasted image 20250908085116.png]]
- 问题定义
  - n个物品组成的集合O，每个物品有两个属性$p_i和v_i$ 分别表示饮品的价格和体积
  - 背包容量为C
![[Pasted image 20250908085359.png]]
在上面例子中，我们计算每个饮料的性价比
![[Pasted image 20250908090615.png]]
优先选择性价比最高的饮料
**如何证明？**
- 正确性证明
  贪心策略：最高性价比优先
  证明：贪心解不劣于最优解

- 伪代码
  ![[Pasted image 20250908102229.png]]
### 霍夫曼编码


## 图

### 广度优先搜索
![[Pasted image 20250908113141.png]]
- 核心思想：处理某节点时同时发现相邻的所有顶点
  Q：若发现多个节点，但一次只能处理单个节点？
  利用队列先进先出的特性，将未处理节点加入等待队列
- 辅助数组
  利用辅助表示顶点状态
  - white：表示顶点u还没有被发现，发现后直接入队
  - black：顶点u已被处理，不用再次入队
  - gray：表示顶点u已经在队列中（之前已经被发现过）



### 强联通分量图
1.Kosaraju 算法
主要思想：两次dfs
	第一次dfs：对反向图进行dfs，得到每个顶点的“完成时间”顺序order数组（在后面的第二次dfs中按照这个顺序遍历原图）
	第二次dfs：对原图按顺序dfs，每次遍历都要弹出栈顶元素（直接对order数组逆序遍历），每次遇到没有访问的点就dfs，形成一个scc，加入结果集，接着再遍历剩下没有遍历的点
	
细节处理：在第二次dfs中，创建一个component数组，将遍历的顶点添加到这个数组里，最后形成一个强联通分量。
2.强联通分量图$G^{scc}$ 
	性质：强联通分量图肯定是有向无环图。
	如果把图中的强联通分量看成一个点，如果存在环路，则这两个点之间联通，记这两个scc可以互相到达，和scc的定义矛盾。
### 迪杰斯特拉算法
1.算法介绍
	dijkstra算法是一种解决单源最短路径且边权为正的问题，计算从源点到其他顶点的最短路径。
主要思想：
	根据贪心策略对当前没有遍历，距离源点(rec)最近的点进行确定。
	再对这个确定的点去尝试对它所有的邻居进行松弛操作。
	重复到所有点都被确定，没有可达点。
正确性证明：
	定理：迪杰斯特拉算法中，顶点u被添加到$V_A$(最短路径的集合)，dist[u] = $\delta$ (s, u)
	![[Pasted image 20250722230220.png]]
	适用于边权非负的有向无向图，使用优先队列的数据结构，时间复杂度为O(|E|log|V|)

### Bellman-Ford算法
1.算法介绍
	用于处理负权边的图。
	问题：如果存在负权边，是否存在单源最短路径？
	可能会通过松弛操作将每个顶点更新到负无穷，形成负环。
	如果源点无可达负环，则存在源点的最短路径。
2.算法原理
	主要两大部分
	·松弛操作：进行n-1次松弛操作，对每个顶点进行更新
	·检测负权图：如果图中存在一条环路使得边的权重之和为负，则没有最短路径。
正确性证明：

算法时间复杂度为O(|E* V|)
