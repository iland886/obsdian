---
id: "2"
title: 盛最多水的容器
difficulty: 中等
attempts: 0
topics: 双指针
notes: 
tags:
  - LeetCode
link:
---

## 📘 题目描述

给定一个长度为 `n` 的整数数组 `height` 。有 `n` 条垂线，第 `i` 条线的两个端点是 `(i, 0)` 和 `(i, height[i])` 。

找出其中的两条线，使得它们与 `x` 轴共同构成的容器可以容纳最多的水。

返回容器可以储存的最大水量。

---

## 🧠 解题思路

- 解法一（暴力）：两层for循环遍历i,j计算出最大面积
- 解法二（优化）：![[Pasted image 20250809230104.png]]
- 核心算法思想：双指针 

---

## 💻 代码实现（C++）

```cpp
class Solution {

public:

    int maxArea(vector<int>& height) {

        int i = 0, j = height.size() - 1;

        int res = 0;

        while (i < j)

        {

           res = max(res,(j - i)*min(height[j], height[i]));

           if (height[i] < height[j])

           {

            i++;

           }

           else

           {

            j--;

           }

        }

        return res;

    }

};
